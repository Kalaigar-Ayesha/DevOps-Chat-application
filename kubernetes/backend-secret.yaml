# Note: In production environments, this Secret object is automatically 
# provisioned and managed dynamically by External Secrets Operator (external-secret.yaml)
# pulling from AWS Secrets Manager / Vault.
apiVersion: v1
kind: Secret
metadata:
  name: backend-secret
type: Opaque
stringData:
  MONGODB_URI: "mongodb://mongo:27017/chatapp"
  JWT_SECRET: "chatapp_development_secret_change_in_production"
  CLIENT_URL: "http://localhost:5173"
  CLOUDINARY_URL: ""